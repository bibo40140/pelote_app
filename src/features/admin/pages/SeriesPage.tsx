import { useEffect, useState } from 'react'
import { useForm } from 'react-hook-form'
import { AdminNav } from '../components/AdminNav'
import { useReferences } from '../hooks/useReferences'
import type { Series } from '../api/referenceApi'
type FormValues = { name: string; sort_order: number }
export function SeriesPage() { const { archive, create, data, error, isLoading, update } = useReferences<Series>('series', 'id, name, sort_order, active'); const { handleSubmit, register, reset } = useForm<FormValues>(); const [editingId, setEditingId] = useState<string | null>(null); useEffect(() => { if (create.isSuccess || update.isSuccess) reset() }, [create.isSuccess, reset, update.isSuccess]); async function submit(values: FormValues) { const payload = { ...values, name: values.name.trim() }; if (editingId) { await update.mutateAsync({ id: editingId, values: payload }); setEditingId(null) } else await create.mutateAsync(payload) } return <section><AdminNav /><h1 className="text-3xl font-semibold">Séries</h1><form className="mt-6 grid gap-3 sm:grid-cols-3" onSubmit={handleSubmit(submit)}><input className="border border-stone-300 bg-white px-3 py-2" placeholder="Nom" required {...register('name', { required: true })} /><input className="border border-stone-300 bg-white px-3 py-2" min="0" required type="number" {...register('sort_order', { required: true, valueAsNumber: true })} /><button className="bg-teal-800 px-4 py-2 text-sm font-semibold text-white" type="submit">{editingId ? 'Modifier' : 'Créer'}</button></form>{error ? <p className="mt-3 text-sm text-red-700">{error.message}</p> : null}{isLoading ? <p className="mt-6 text-sm text-stone-600">Chargement...</p> : <table className="mt-6 w-full text-left text-sm"><thead><tr><th>Nom</th><th>Ordre</th><th>Statut</th><th /></tr></thead><tbody>{data?.map((item) => <tr className="border-b border-stone-200" key={item.id}><td className="py-3">{item.name}</td><td>{item.sort_order}</td><td>{item.active ? 'Active' : 'Archivée'}</td><td className="flex gap-3 py-3"><button className="text-teal-800" onClick={() => { setEditingId(item.id); reset({ name: item.name, sort_order: item.sort_order }) }} type="button">Modifier</button>{item.active ? <button className="text-red-700" onClick={() => archive.mutate(item.id)} type="button">Archiver</button> : null}</td></tr>)}</tbody></table>}</section> }